local lib = {}

---Create spidertron legs
function lib.create_leg (
		spidertron_name,
		scale,
		initial_movement_speed,
		movement_acceleration,
		number,
		animation
	)
	local legs = {}
	-- TODO add in creation of spidy legs for a spidertron
	for i = 1, number do
		local 
		table.insert(legs, {
			
		})
	end

	return legs
end

return lib